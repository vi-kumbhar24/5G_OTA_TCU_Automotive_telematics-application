#ifndef AUTONET_TYPES_H
#define AUTONET_TYPES_H

#include <string>
#include <vector>
#include <map>
using namespace std;

typedef vector<string> Strings;
typedef map<string,string> Assoc;
typedef unsigned char Byte;
typedef unsigned char Bit;

#endif
